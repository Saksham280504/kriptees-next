/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['via.placeholder.com',
       'your-cdn.com',
       'res.cloudinary.com'
      ], // replace with your actual image domains
  },
};

export default nextConfig;
